export {
  ExpandedPanel,
  CollapsedRail,
  // The panel's ground and its right edge travel with this barrel, so anything
  // that wants to sit on the same navy imports them rather than restating one.
  PANEL_BG,
  PANEL_EDGE,
  Sidebar as default
} from '../components/Sidebar/Sidebar';
